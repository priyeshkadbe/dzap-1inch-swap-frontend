import { ethers } from 'ethers';

const convertGasPriceToTokenAmount = (gasPrice:number, tokenDecimals:number) => {
  // const gasPriceBN = BigInt(gasPrice);
  // const tokenAmountBN = gasPriceBN * BigInt(10) ** BigInt(tokenDecimals);

  // const tokenAmount = ethers.formatUnits(
  //   tokenAmountBN.toString(),
  //   tokenDecimals,
  // );

  // return tokenAmount;
};
