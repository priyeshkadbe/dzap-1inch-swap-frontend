// export default function formatNumber(number: any) {
//   const absoluteNumber = Math.abs(number); // Ensure the number is positive

//   // If the number is less than 1, show all decimal places
//   if (absoluteNumber < 1) {
//     return number.toFixed(15).replace(/\.?0+$/, ''); // Trim trailing zeros
//   }

//   // If the number is greater than or equal to 1, show up to 5 decimal places
//   return number.toFixed(5).replace(/\.?0+$/, ''); // Trim trailing zeros
// }
